export type UserProfileType = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  profilePicture: string;
};
export interface Group {
  title: string;
  description: string;
  interval: string;
}
